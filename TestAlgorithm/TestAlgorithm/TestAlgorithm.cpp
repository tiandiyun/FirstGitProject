// TestAlgorithm.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"
#include "RedWars/RedWars.h"
#include <stdlib.h>
#include <iostream>
#include <string>
#include <memory>

int _tmain(int argc, _TCHAR* argv[])
{
    /*for (int i = 0; i < 100; ++i)
    {
        CreateRedPackage4(100, 10);
    }*/

    //float a = 1545901284;
    
    /*int amount = 0;
    std::string buffer;
    while (std::getline(std::cin, buffer))
    {
        try
        {
            amount = std::stoi(buffer);
        }
        catch (...)
        {
           break;
        }
        
        CompareCrateOrderList(amount);
    }*/
   

    system("pause");
	return 0;
}

